name: Saatlik IPTV Güncelleme

on:
  schedule:
    # Her saat başı çalışır
    - cron: '0 * * * *'
  workflow_dispatch: # Manuel tetikleme butonu

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu Çek (Checkout)
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # HATA ÇIKARAN KISMI DEĞİŞTİRDİK
      # Artık requirements.txt dosyasına bakmadan direkt requests yüklüyor.
      - name: Kütüphaneleri Yükle
        run: |
          pip install requests

      - name: Scripti Çalıştır
        run: python scraper.py

      - name: Değişiklikleri Kaydet ve Gönder (Commit & Push)
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          git add iptv_list.m3u
          # Değişiklik yoksa hata vermemesi için önlem
          git commit -m "Otomatik liste güncellemesi: $(date)" || exit 0
          git push
